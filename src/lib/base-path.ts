// Mirrors next.config.ts's basePath. Only non-empty for the GitHub Pages
// preview build; empty ("") for the SiteGround production build.
export const basePath = process.env.NEXT_PUBLIC_BASE_PATH || "";
